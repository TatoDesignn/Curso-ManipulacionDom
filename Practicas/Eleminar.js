const container = document.querySelector('.Padre');
const hijo = document.querySelector('.hijo');

container.removeChild(hijo);

const container2 = hijo.parentElement;

container.remove

container.replaceChild(variable1, remplazo);